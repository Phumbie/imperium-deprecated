export const batteries = (state) => {
  return state.batteries;
};

export const inverters = (state) => {
  return state.inverters;
};

export const panels = (state) => {
  return state.panels;
};

export const bundles = (state) => {
  return state.bundles;
};

export const loading = (state) => {
  return state.loading;
};

export const productsList = (state) => {
  return state.productsList;
};

export const pagination = (state) => {
  return state.pagination;
};

export const page = (state) => {
  return state.page;
};
